#include <Arduino.h>
#include <TFT_eSPI.h>
#include "drone_model.h"

// Paste the long array outputs containing the raw numbers here
const Point3D vertices[TOTAL_VERTICES] = {
  // Array maps generated from Python output...
};

const Face faces[TOTAL_FACES] = {
  // Face maps generated from Python output...
};